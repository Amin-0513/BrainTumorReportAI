name: llm-gpu
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - cudatoolkit=11.8

# After creating this environment, run:
#   pip install -r requirement.txt
# And install PyTorch with the matching CUDA support (example for CUDA 11.8):
#   conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia
